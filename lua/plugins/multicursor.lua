return {
  {
    "mg979/vim-visual-multi",
    init = function()
      -- Define your initial keybindings before the plugin loads if needed
      -- Example: change the main trigger from default <C-n> if it conflicts
      -- vim.g.VM_maps = { ['Find Under'] = '<C-d>' }
    end,
  },
}
